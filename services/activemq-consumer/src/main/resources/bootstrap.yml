server:
  port: 10501

spring:
  application:
    name: activemq-consumer
  profiles:
    active: dev

logging:
  level:
    com.gsww.feign.HcpRestFeign: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"